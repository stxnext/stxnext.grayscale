stxnext.grayscale
=================

# TODO:
# 1. configureable transformable skin names, (DONE)
# 2. bring back compatibility with Plone 3 (make importing of plone.resource.file import FilesystemFile as optional) (DONE)
# 3. configureable transformable diazo theme names, (NOT NEEDED AFTER CONSIDERATION)
# 4. transformating other (than hex) conventions of colors definitions
# 5. rename the images paths to avoid browser cache
# 6. save the transformed images in ZODB and fallback to them if possible
# 7. solve the problem of displaying flash and external images